# Archived Implementations

These directories preserve previous AMR work for reference.

- `legacy-vue/`: Vue, Vuex, Webpack, reader, and mirror implementation
- `parity-svelte/`: unfinished Svelte parity rewrite
- `platform-prototype/`: paused API, web, and worker prototype
- `stable-reference/`: selected stable reader files

Archived code is not built by the root workspace and must not be imported by active
packages.

The complete workspace before cleanup is also preserved in commit `1d032ef0` on branch
`archive/pre-clean-rewrite`.
